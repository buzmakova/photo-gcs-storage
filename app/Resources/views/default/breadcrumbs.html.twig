<ol class="breadcrumb">
    {% for item in breadcrumbs %}
        {% if loop.last %}
            <li class="active">{{ item.name }}</li>
        {% else %}
            <li><a href={{ item.link }}>{{ item.name }}</a></li>
        {% endif %}
    {% endfor %}
</ol>